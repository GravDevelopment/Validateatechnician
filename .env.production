# Production build talks straight to the existing kiosk function app.
# VITE_API_KEY is NOT set here on purpose - it is injected from the
# VITE_API_KEY repo secret by .github/workflows/pages.yml, so the key
# stays out of this public repo's git history.
VITE_API_BASE=https://gravity-learner-kios-g5ghdwfwe4gkbxgm.southafricanorth-01.azurewebsites.net
